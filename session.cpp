#include "session.hpp"

/*
  std::ostream& operator<<(std::ostream& os, EVENTS e) {
    switch (e) {
    case (EVENTS::L):
      os << "LOGIN_REQUEST";
      break;
    case (EVENTS::E):
      os << "LOGIN_RESPONSE";
      break;
    case (EVENTS::O):
      os << "LOGOUT_REQUEST";
      break;
    case (EVENTS::G):
      os << "LOGOUT_RESPONSE";
      break;
    case (EVENTS::S):
      os << "SUBMISSION_REQUEST";
      break;
    case (EVENTS::R):
      os << "SUBMISSION_RESPONSE";
      break;
    default:
      os << "UNKNOWN EVENT";
      break;
    }
    return os;
  }
*/
